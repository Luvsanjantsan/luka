SELECT m.LastName, m.FirstName, m.MemberId 
FROM Member m INNER JOIN Entry e ON m.MemberID = e.MemberID 
GROUP BY m.MemberID, LastName, FirstName 
HAVING 
COUNT(DISTINCT e.Year) = (SELECT COUNT(DISTINCT e.Year) FROM Entry e);
